{
  "name": "pigeon-ballpit",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A second Pigeon ISP visualizer: a Rokenbok-style ball factory. Every frame is a ball; you build the router out of chutes, launchers and ball pits. Independent app on the same @pigeon/protocol engine.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
